<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.kyuuki.rensou.android"
    android:versionCode="5"
    android:versionName="1.0.3a" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/U1Theme" >
        <activity
            android:name="jp.kyuuki.rensou.android.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/NormalActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="jp.kyuuki.rensou.android.PostResultActivity"
            android:label="@string/activity_result_title"
            android:theme="@style/NormalActivityTheme">
        </activity>
    </application>

    </manifest>
